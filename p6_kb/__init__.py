"""Rule-based Constructability Review — engine + Construction Knowledge Base.

The v1.0 (Community Edition) constructability engine: it reviews a schedule against
a **local, editable Construction Knowledge Base** (per project sub-type) using rules
only — no external AI, fully offline, zero operating cost (Decision 009). The AI
version lives dormant in ``p6_ai`` as a future-Pro module.

Nothing here imports ``p6_ai``; nothing in ``p6_evm`` / ``p6_audit`` imports this.
"""
